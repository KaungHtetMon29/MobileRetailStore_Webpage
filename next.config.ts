import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['www.placehold.co', 'www.via.placeholder.com', 'www.istudio.store'],
  }
};

export default nextConfig;
